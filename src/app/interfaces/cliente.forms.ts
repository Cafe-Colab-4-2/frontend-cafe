export interface ClienteForm {
    nombre : string,
    nit: string,
    email  ?: string,
    telefono  ?: string,
    direccion  ?: string,
    img ?: string,
    activo  ?: string,
    usuario ?: string,
    apellido?: string
}